buildscript {
    ext {
        springBootVersion = "2.2.6.RELEASE"
        springDependencyManagementVersion = "1.0.9.RELEASE"
        lombokVersion = "1.18.12"
        dockerImagesPrefix = "template"
    }
}
